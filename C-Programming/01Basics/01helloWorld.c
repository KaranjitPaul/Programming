#include <stdio.h> // Gives us access to printf()

int main(void) { // The program starts running from main()
    printf("Hello World\n"); // Text inside double quotes is a string
    return 0; // Tells the operating system that the program ended successfully
}
